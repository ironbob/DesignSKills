# 06-组件覆盖映射与样张记录（阶段 6）

## 组件覆盖核对（04 清单 → 底座组件）

| 04 内容块 | 类型 | 底座件 | 状态 |
|---|---|---|---|
| s1 核心成绩单 / s6 POC 成绩单 | T13 | `KpiRow`（字号已变量化 --doc-kpi-num-size） | ✅ 已有 |
| s1/s4 论点与对比结论 / s2 痛点 / s5 口径 | T10 | `Callout`（conclusion / risk / data 三型齐） | ✅ 已有 |
| s2 约束表 / s3 选型总表 / s5 原值表 | T11 | 原生 markdown 表（VitePress 排版） | ✅ 已有 |
| s4 落选对比矩阵 | T5 | `CompareMatrix` | ✅ 已有 |
| s5 五指标改善幅度 | T6 | `FigureChart` + **新增构造器 `rankBars`**（排序横向柱：值标柱端/重点柱强调） | ✅ 本阶段补构造器 |
| s3 架构分层 / s6 时序 / s7 流程 | T3/T2/T1 | Mermaid 插件（真实三图已在样张预演） | ✅ 已有 |
| s7 Q4 排期 | T12 | **新增组件 `Timeline`**（HTML/CSS 时间线，SSG 直出无 JS 依赖，accent 关键路径） | ✅ 本阶段定向扩展 |
| 页头 | — | `.doc-head` 样式类 | ✅ 已有 |

映射空洞：无。一次性组件未进内容目录（Timeline/rankBars 均入底座层）。

## 档位应用

- 选定档位=**简报档 brief**（阶段 5 拍板）；正文页以 `body[data-doc-preset='brief']` 应用。
- chartTheme token 与档位同源（accent #2563EB ↔ --doc-accent #1d4ed8 家族），图表内部观感由 withTheme 统一注入，option 不写颜色/字号裸值。

## 样张验证（build + preview 实测，2026-08-23）

- 样张页：`doc-renderer/docs/danci-data-layer/sample.md`（过程产物，定稿前移除出构建）
- DOM 实测：preset=brief、light 模式；KpiRow ×1、ECharts SVG ×1（降级文本 0 外露）、Mermaid SVG ×3、CompareMatrix ×1、原生表 ×1、Timeline 节点 ×3（含 accent ×1）、Callout 三型齐
- `check_doc.py --md`：0 ERROR / 1 WARN（script 块被误判段落，过程页可接受）
- `npm run build`：0 退出；截图归档 `05-style-tiles/sample-brief.png`
