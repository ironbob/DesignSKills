# 阶段 9：交付规格导出（任务卡）

把全部过程产物组装成**自包含设计规格**——编码智能体（或工程师）拿它与 tokens + 高保真参照即可开工，不需要翻任何过程文件。

## 产出（`09-spec.md`，十节）

1. **消费者与契约范围**：本文 + `06-tokens.json` + `07-hifi/` 为契约；其余为过程产物可作背景。
2. **设计前提**：决策台账的最终快照（表格：决策/值）——全部已拍板项，含变体选择与 P4-x 修订。
3. **IA 与导航**：Mermaid IA + 转场表（引用并入，不外链）。
4. **领域语义**：语义态三元组表（色/符号/文字）。
5. **逐屏规格**：每屏 job / 结构（自上而下区块）/ 状态×呈现 / 交互与规则挂接（R#）/ 文案要点 / **参照帧文件名**（07-hifi/S#.html）/ 验收点。
6. **核心屏模式×状态矩阵**：全文嵌入。
7. **设计系统契约**：token 数据源指针 + 规则要点 + 组件样张指针。
8. **实现注意**：mock 与真渲染的边界、时基/性能约束、safe-area、并发回归项、待定参数。
9. **未决决策位 U-x**：每个给"编码按倾向实现 + 可切换"的处理。
10. **验收标准 + 产物索引**：设计层成功标准（阶段 1 定的）+ 契约/过程文件分类表。

## 完成标志（L1 gate）

十节结构齐全（≥8/10 节命中）；声明契约三件套且真实存在；spec 引用的每个 `07-hifi/*.html` 参照帧真实存在（自包含性抽查）。

## 工具化要点

- 决策类型=confirm（最终交付确认）。事实类：auto 过双层 gate 自动推进，推进后项目九阶段全 done。
- 这是"导出"的文件形态：契约=spec + tokens + 参照帧，可直接交给下游编码智能体。
