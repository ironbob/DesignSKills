# 阶段 3：屏幕清单 + 信息架构（任务卡）

从**流程停留点**推导屏幕（不是从功能清单分解），分级挂载，并盘点每屏内容。

输入：`02-流程草图.md`（已确认）。

## 产出（写入 `03-屏幕与IA.md`）

1. **IA 图**（Mermaid flowchart）：分层——导航壳（Tab/侧栏）/ Tab 落点页 / 二级页 / 全屏沉浸层 / 浮层弹层。沉浸型核心屏（练习/编辑/播放类）不是 Tab，是跨入口的全屏层。
2. **屏幕盘点表**（card-sorting 粒度）：`| ID | 屏幕 | 层级 | 信息内容 | 操作（**主**加粗） | 服务流程 | 模式倾向 |`。领域特有版式超出常规枚举时如实标注"超出模式库，阶段 4 定义"。
3. **闭环检查表**：每条流程 → 覆盖屏幕列表 → ✅；孤儿屏检查（每屏被 ≥1 流程触达）。
4. **关键屏提名**（2–3 个，阶段 4 只做这些）：标准=状态机最复杂屏 / 第一印象屏 / 风险最高交互屏，各带一句理由。
5. **新问题 P3-x**（带倾向）。

## 完成标志（L1 gate）

闭环检查全 ✅；关键屏 2–3 个且都在盘点表内；盘点表 ≥3 屏且无孤儿屏（每屏在 IA 图或闭环中出现 ≥2 次）；屏幕数 ≤12。

## 工具化要点

- 决策类型=confirm（认 IA 分层/屏幕清单/关键屏提名）；auto 模式过双层 gate 自动推进，P3-x 按倾向记台账。
- 下一阶段（关键屏灰框）从「关键屏提名」小节读 S# 名单。
