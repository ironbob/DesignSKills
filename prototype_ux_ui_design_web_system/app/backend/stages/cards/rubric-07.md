# 阶段 7 评审判据卡：高保真

评审对象：`07-hifi/`（全量屏 + index.html）。上游锚点：`06-tokens.json`、阶段 4 已选线框、08 前置的模式×状态矩阵行。先读 `review-conventions.md`。
决策类别：**事实类**（对齐四问有结构判据；视觉品质对照 token 可判）。
注：L1 `check_artifacts.py` 验标签/画布/文案；状态覆盖由 L1 矩阵核对验。评审不重复。

## 判据

| # | 判据（离散，三值判定） | 级别 |
|---|---|---|
| R7-1 | 关键屏先行且有对齐点清单：强调克制度 / 层次差可感 / 语义色在底色上可辨 / 浮层手法——四问有问有答 | 🔴 |
| R7-2 | 零新增 token：所有色值/字号/间距/圆角能在 06-tokens.json 找到出处（抽屏逐值核对） | 🔴 |
| R7-3 | 零新增组件：全部从阶段 6 组件族组装（新形态出现=命中，除非声明 P4-x） | 🔴 |
| R7-4 | 每屏含状态帧：default + 领域状态（空/加载/错误/异常变体/置灰规则），对照矩阵行无缺帧 | 🔴 |
| R7-5 | 状态语义正确："无信号"≠"坏"（无信号不渲染成 danger 红），对照语义三元组 | 🔴 |
| R7-6 | 结构未动：与阶段 4 已选线框逐屏对比，布局分叉未经 P4-x 备案=命中 | 🔴 |
| R7-7 | 已知简化有声明：mock 无法真渲染的领域内容注明"真渲染是编码阶段职责" | 🟡 |
| R7-8 | 危险操作：深色呈现 + 二次确认（对照 rules） | 🔴 |
| R7-9 | 触控目标符合 touch 下限（主要按钮/行/键） | 🟡 |
| R7-10 | index.html：全屏索引 + 契约执行情况（画布/键数/组件来源）+ 已知简化声明 | 🟡 |
| R7-11 | 文案与 03 盘点内容一致，无 lorem/占位（L1 验负面词，此处验内容归属） | 🔴 |

## 常见命中示例

- R7-2 命中：`S4 用了 #5B8DEF，tokens 中无此值（未经 token 的自由上色）`
- R7-5 命中：`无信号帧整体红字+✕，与 danger 语义混用`
- R7-6 命中：`S2 顶栏从线框的吸顶变为随内容滚动，无 P4-x 台账记录`

## 放行

🔴=0 且覆盖完整 → auto 模式：全量屏生效记台账，自动进阶段 8。
适配类问题（挤/溢出）→ 触发 P4-x 结构修订流程，线框与高保真同步改。
