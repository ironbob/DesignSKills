# 阶段 4 评审判据卡：关键屏低保真

评审对象：`04-wireframes/`（含 index.html）。上游锚点：`03-屏幕与IA.md` 关键屏名单。先读 `review-conventions.md`。
决策类别：**生成质量=事实类可评；挑变体=品味类必停人工**。评审可附变体对比分析（各自取舍）降低人工决策成本，但**不许替用户选**。
注：标签配平/画布锁定/负面文案由 L1 `check_artifacts.py` 验，评审不重复。
模式感知（引擎注入，评审器不自选）：rapid 下 R4-1/R4-2（两套结构候选与假变体判别）按 not_applicable 处理（理由=rapid 单候选），不得判红；替代验证=单套方案有清晰依据（帧下标注布局模式/取舍，台账 rapid_default 可追溯）。其余判据照常三值判定。deliberate 下本卡判据完全不变。

## 判据

| # | 判据（离散，三值判定） | 级别 |
|---|---|---|
| R4-1 | 阶段 3 提名的每个关键屏都有 v1/v2 两文件（名单逐一核对） | 🔴 |
| R4-2 | 变体差异在**布局结构**（反馈位置/面板形态/组织方式级分叉），非同布局微调（假变体=命中） | 🔴 |
| R4-3 | 每屏状态帧覆盖该屏的模式×状态，**含异常态**（空/中断/错误——对照 03 盘点行与 02 状态机） | 🔴 |
| R4-4 | 帧下灰字标注是设计理由（为什么这样摆），不是内容文案 | 🟡 |
| R4-5 | 全部 wireframe token：纯灰、无图片、图标用文字 | 🔴 |
| R4-6 | 真实感数据从灰框起就有（真实人名/曲目/金额，非"条目1"） | 🔴 |
| R4-7 | 触控目标 ≥48px 画够（抽查主要按钮/行高） | 🟡 |
| R4-8 | index.html 为变体对照板：每对变体写明**要决策的问题**（"X 放哪：贴 A 还是固定 B？"式） | 🔴 |
| R4-9 | 布局模式可回指 03 的模式倾向（或已声明"超出模式库"的理由） | 🟡 |
| R4-10 | 未决定颜色字体质感：无彩色值/字体族出现（灰框越权=命中） | 🔴 |

## 常见命中示例

- R4-2 命中：`S2-v1 与 v2 仅列表行高不同，区域结构完全一致（假变体）`
- R4-3 命中：`练习屏只有进行中帧，无中断恢复/错误帧——02 的状态机心脏 subgraph 声明了 4 态`
- R4-8 命中：`index.html 只有缩略图对照，未写决策问题`

## 放行

🔴=0 且覆盖完整 → 停人工挑变体（品味类，auto 模式同样停）；拍板记台账（S-x=V?，可混搭）。
