# 阶段 6 评审判据卡：设计系统

评审对象：`06-tokens.json` + `06-design-system.html`。上游锚点：阶段 5 选中 tile、阶段 4 结构决策。先读 `review-conventions.md`。
决策类别：**事实类**（构成完备性可判；语义色可辨度有结构判据）。

## 判据

| # | 判据（离散，三值判定） | 级别 |
|---|---|---|
| R6-1 | tokens.json 键齐：color 九键 / semantic / type（含 scale）/ space / radius / touch / rules（L1 验结构，此处验语义：值与选中 tile 一致，无静默换向） | 🔴 |
| R6-2 | 每个 semantic 态是三元组 `{color, symbol, label}`，符号+文字强制 | 🔴 |
| R6-3 | `focus_surface` 全局唯一最亮（逐值比对，只服务核心内容区） | 🔴 |
| R6-4 | rules 十二条底版逐条在（核心区恒亮/一屏一强调/语义色只干语义/符号+文字/标题族只做标题/8 倍数留白/画布固定/触控下限+主键宽+键数上限/核心字号下限/无信号≠坏/危险深色+二次确认/浮层居中） | 🔴 |
| R6-5 | "无信号"态未进语义三色（与 danger/pass 类区分） | 🔴 |
| R6-6 | space 数组全 8 倍数；scale 值为常见字号且 focus_size ≥ 领域可读下限 | 🟡 |
| R6-7 | 危险色与语义色可区分（色值距离肉眼可辨，非邻近色） | 🔴 |
| R6-8 | 样张组件族覆盖：按钮 3–4 式 / 列表行 / 分组头 / 分段 / 字段 / 面板 / 卡片 / **核心内容族**（单独立规范）/ 顶栏 / 徽章 | 🔴 |
| R6-9 | 缺件自检：非关键屏逐屏核对"构成元素全部能在组件族找到"有结论 | 🔴 |
| R6-10 | 样张视觉与 tokens 无漂移（样张里的色值/圆角/间距能在 json 找到出处） | 🟡 |
| R6-11 | 样张末尾十条原则在 | 🟡 |

## 常见命中示例

- R6-3 命中：`surface #ffffff 与 focus_surface #ffffff 同值，核心区不再唯一最亮`
- R6-5 命中：`semantic["无信号"].color 与 danger 同为红系且符号同为 ✕`
- R6-9 命中：`S7 统计屏的"趋势图"在组件族无对应件，未声明缺件`

## 放行

🔴=0 且覆盖完整 → auto 模式：token 生效记台账，自动进阶段 7。
tokens.json 此刻起为项目 token 表，后续高保真任务只读消费。
