# 阶段 7：高保真（任务卡）

先关键屏上视觉→**对齐**→才铺全量屏+状态变体。上视觉时结构不动；要动结构，回阶段 4 备案 P4-x 后线框高保真同步改。

输入：`06-tokens.json`（含项目画布）+ 阶段 4 已选线框。

## 产出（`07-hifi/`）

- 关键屏先行：`<screen-id>.html`（多状态帧，token 上视觉），交**对齐点清单**（如"强调克制度成立吗／层次差可感吗／语义色在底色上可辨吗／浮层手法对吗"）；
- 对齐通过后铺全量：03 盘点表每屏一文件（`S#.html`），**每屏含状态帧**（default + 领域状态：空/加载/错误/异常变体/置灰规则），全部从阶段 6 组件族组装——**零新增组件零新增 token**；
- `index.html`：全屏索引 + 契约执行情况（画布/键数/组件来源）+ 已知简化声明。

**已知简化必须声明**：mock 无法真渲染的领域内容，用简化示意并注明"真渲染是编码阶段职责，不影响本阶段评审布局层级与状态语义"。

## 完成标志（L1 gate）

03 盘点表每屏在 07-hifi/ 有文件且含状态帧；index.html 存在；全部 HTML 过三查 gate（画布=项目画布，宽高都锁）。

## 工具化要点

- 决策类型=confirm（关键屏对齐四问→放行铺全量；适配类问题触发 P4-x 结构修订）。事实类：auto 过双层 gate 自动推进。
- 状态语义混用（"无信号"渲染成"坏"）= crit 🔴 最大来源，对照阶段 8 矩阵逐格画。
