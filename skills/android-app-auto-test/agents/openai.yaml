interface:
  display_name: "Android App Auto Test"
  short_description: "Run scoped native Android tests"
  default_prompt: "Use $android-app-auto-test to analyze or directly run a scoped native Android test plan on a real device, then fix failures."
