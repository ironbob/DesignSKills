interface:
  display_name: "UI Layout QA"
  short_description: "Audit and repair UI layout with scoped evidence"
  default_prompt: "Use $ui-layout-qa to inspect this UI change for layout, content density, and state issues; repair the affected scope and verify it."
policy:
  allow_implicit_invocation: true
