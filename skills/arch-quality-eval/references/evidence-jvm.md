# JVM 架构证据

- 用 package、目录和类型职责识别边界；目录名只作线索。
- 用 import、字段/参数类型、调用和实现关系确认内部依赖边。
- 优先用 LSP 查询关键引用、调用和实现；不可用时文本搜索后阅读源码确认。
- 可见性只在泄漏易变实现或扩大非必要契约时支持信息隐藏 finding。
- Java/Kotlin 语法、格式、框架注解风格和语言技巧不在评价范围。
- 不为本诊断额外运行 javac、Gradle/Maven 构建或测试；构建状态不进入 finding 或报告结论。
