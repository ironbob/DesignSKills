---
business: order-refund
title: 订单退款功能需求（反推）
summary: 用户对已支付订单发起退款，系统校验可退条件后调用第三方支付退款，并更新订单状态为已退款
codebase: order-service
entry_type: http
analyzed_at: 2026-06-16
status: example
source_analysis: 2026-06-16-order-refund-analysis.md
reverse_engineered: true
gaps: 2
---

# 订单退款功能需求（反推）

> 示例产出（代码路径为虚构），从同组 `analysis.md` 反推的 PM 视角需求，源自 `code-analyze-business` skill。结构与 `clarify-requirements` 正向需求对齐，多出「实现状态」列与「实现与需求偏差」节。

## 1. 一句话目标

让已付款但需要撤销交易的用户，能自助发起退款，系统自动校验可退条件并完成资金退还。 —— 见 analysis §1 业务概述

## 2. 目标用户

- 主要用户：已支付订单的 C 端用户（想取消已付款交易）。
- 次要用户：处理退款异常的运营/客服（介入补偿队列里的失败退款）。
- **不是**目标用户：未支付订单的用户（无款可退）、商家后台（退款发起方是用户而非商家）。

## 3. 核心场景

- 正常退款：用户在"订单详情 → 申请退款"点击 → 系统校验订单可退 → 调第三方退款 → 订单变"已退款"。 —— analysis §4
- 不可退拦截：用户对非"已支付"订单点退款 → 系统拒绝，提示原因。 —— analysis §5
- 退款失败兜底：第三方退款失败 → 系统记录失败并入补偿队列，不直接报错给用户。 —— analysis §5

## 4. 功能清单

### 模块：退款发起
| 优先级 | 功能 | 简述（做什么） | 验收标准（可判定） | 实现状态 | 回链 |
|--------|------|----------------|-------------------|----------|------|
| P0 | 发起退款 | 用户对已支付订单提交退款申请 | 接口 `POST /api/orders/{id}/refund` 接收 order_id + 金额 | ✅ 已实现 | analysis §2 / `src/api/refund.py:42` |
| P0 | 可退校验 | 校验订单是否满足退款条件 | 仅状态∈{已支付(1)}的订单可退；不可退返回 400 + `OrderNotRefundableError` | ✅ 已实现 | analysis §5 / `src/service/refund_service.py:95` |
| P0 | 计算退款金额 | 按规则算全额或部分退款 | 退款金额 ≤ 可退余额；超出则拒 | ⚠️ 部分 | analysis §4 / `src/service/refund_service.py:110` |
| P0 | 调第三方退款 | 调支付中心执行真实退款 | 调用 `PaymentClient.call_refund`；超时 3s、失败重试 2 次 | ✅ 已实现 | analysis §7 / `src/clients/payment_client.py:55` |
| P0 | 更新订单状态 | 退款成功后置订单为已退款 | 退款成功 → orders.status 置为已退款(4) | ✅ 已实现 | analysis §6 / `src/repo/order_repo.py:120` |

### 模块：可靠性
| 优先级 | 功能 | 简述（做什么） | 验收标准（可判定） | 实现状态 | 回链 |
|--------|------|----------------|-------------------|----------|------|
| P0 | 退款幂等 | 同一订单重复/并发退款不双退 | Redis 锁 key=order_id 串行化 + refund_records.order_id 唯一索引 | ✅ 已实现 | analysis §8 / `src/service/refund_service.py:160` |
| P0 | 失败补偿 | 第三方退款失败后兜底 | 失败置退款单"失败"并入补偿队列，不抛给用户 | ✅ 已实现 | analysis §5 / `src/service/refund_service.py:138` |
| P1 | 部分退款拆单 | 一笔订单分多次部分退 | 是否支持拆单未在代码明确 | ❌ 缺口 | analysis §8 ⚠ 未确认 |
| P1 | 补偿重试上限 | 补偿 job 的重试次数/放弃条件 | 补偿逻辑不在本业务内 | ❌ 缺口 | analysis §8 ⚠ 未确认 |

## 5. 明确不做（Out of scope）

- 不做商家代发起退款：退款入口仅 C 端用户，无商家后台入口 —— analysis §2。
- 不做退款审批流：满足可退条件即自动退款，无人工审核环节 —— analysis §4 主流程。

## 6. 非功能约束

- 时效：退款调用第三方超时阈值 3s —— analysis §7。
- 并发：同一订单并发退款须串行化、不双退 —— analysis §8。
- 可靠性：第三方失败重试 2 次，仍失败入补偿队列 —— analysis §7。

## 7. 实现与需求偏差

- **过度实现 / 副作用**：退款失败仅入补偿队列、不向用户报错 —— 用户可能长时间不知退款失败，需求层面是否需要异步通知用户 ⚠ 未确认 —— analysis §5。
- **需求缺口**：补偿队列的重试上限与告警未在本业务内，依赖外部 job —— 一笔退款可能因补偿 job 配置不当而长期挂起 ⚠ 未确认 —— analysis §8。

## 8. 未决问题 / 已知缺口

- **部分退款拆单规则**：`refund_service.py:110` 金额计算未体现是否支持一笔订单分多次部分退，需找产品 / 历史 PR 确认 —— 见 analysis 已知缺口。
- **补偿逻辑归属**：失败后入补偿队列，但补偿 job 不在本业务代码内，未追踪其重试上限 —— 见 analysis 已知缺口。
